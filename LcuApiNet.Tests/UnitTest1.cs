using NUnit.Framework;
using LcuApiNet.Core;

namespace LcuApiNet.Tests
{
    public class Tests
    {
        [SetUp]
        public void Setup()
        {
            // LeagueClientManager uCredentialsManager = new LeagueClientManager();
            // string path = uCredentialsManager.FindClientLocation().Result;
        }

        [Test]
        public void Test1()
        {
            Assert.Pass();
        }
    }
}